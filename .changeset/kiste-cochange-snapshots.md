---
"@vevx/kiste": minor
---

Add `kiste_get_cochange` MCP tool for behavioral coupling queries (co-changing files ranked by frequency with jaccard similarity) and snapshot support for resilient indexing (create/restore via CLI, auto-trigger at configured frequency).
