---
"@vevx/kart": minor
---

Add Rust file support for `kart_replace`, `kart_insert_after`, `kart_insert_before` via tree-sitter AST. Add LSP hover integration for `kart_zoom` — levels 0 and 1 now include `resolvedType` on each symbol with compiler-inferred types.
