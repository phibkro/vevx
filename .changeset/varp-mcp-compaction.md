---
"@vevx/varp": patch
---

Compact MCP tool responses to reduce agent context window usage. Freshness reports only list stale docs, manifest returns component summaries, import scans strip per-file evidence, env checks only list missing vars, and co-change analysis returns summary stats instead of raw edges.
